define(function(require, exports){
    var $ = require('jquery');
    var cookie = require('../util/cookie');
    require('../util/vote');
    // require('../util/share');
    var coupon = require('../util/coupon');
    require('../util/share');
    require('../util/common');
});
